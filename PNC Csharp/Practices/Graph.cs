using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using System.Windows.Forms;
using System.Drawing;

namespace PNC_Csharp.Practices
{
    class Graph 
    {
        /*
        Form1 f1 = (Form1)Application.OpenForms["Form1"];

        internal class Node
        {
            private int node_index;
            public int data;
            public Node(int node_index,int data)
            {
                this.node_index = node_index;
                this.data = data;
            }
        }

        private List<Node> vertices;
        private int[,] adjMatrix;
        private int vertice_amount;
        private const int Infinite_distance = 99999;

        public string Get_Class_Info()
        {
            return "It's directed graph using Shortest-Path Algorithm(Dijkstra,which is a kind of BFS)";
        }

        public Graph(int vertice_amount)
        {
            this.vertice_amount = vertice_amount;
            vertices = new List<Node>();
            adjMatrix = new int[vertice_amount, vertice_amount];
        }

        public void RunDijkstra()
        {
            f1.GB_Status_AppendText_Nextline("***********Dijkstra's Shortest Path***********", Color.Purple);
            int[] distance = new int[vertice_amount];
            int[] previous = new int[vertice_amount];
            for (int i = 1; i < vertice_amount; i++)
            {
                distance[i] = Infinite_distance;
                previous[i] = 0;
            }

            int source = 1;
            distance[0] = 0;
            PriorityQu
            PriorityQueue<int> pq 
            
        }


        */



    }
}
